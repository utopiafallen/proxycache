[project]
name = "proxycache"
version = "0.1.0"
description = "OpenAI-compatible proxy for llama.cpp with KV cache management"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.121.0",
    "httpx>=0.28.1",
    "uvicorn>=0.38.0",
]

[project.scripts]
proxycache = "proxycache:main"
